# Data Structures and Algorithms

- **ECommerceSearch** — Exercise 2: linear vs binary search on products, with Big-O comparison.
- **FinancialForecasting** — Exercise 7: recursive future-value forecasting (+ memoized version).

## Run
```bash
cd ECommerceSearch
javac -d out $(find src -name '*.java') && java -cp out com.example.search.ProductSearch

cd ../FinancialForecasting
javac -d out $(find src -name '*.java') && java -cp out com.example.forecast.FinancialForecast
```
