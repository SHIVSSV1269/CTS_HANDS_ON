# PL/SQL Programming (Oracle)

Run in this order against an Oracle database (SQL*Plus / SQL Developer):

1. `00_schema_and_sample_data.sql` — creates tables and sample rows.
2. `Exercise1_Control_Structures.sql` — Exercise 1: loops + IF conditions (loan discount, VIP flag, due-loan reminders).
3. `Exercise3_Stored_Procedures.sql` — Exercise 3: `ProcessMonthlyInterest`, `UpdateEmployeeBonus`, `TransferFunds`.

Enable output first: `SET SERVEROUTPUT ON;`
